import React from 'react';
import Navbar from '../Components/Navbar';
import '../App.css';

const Home = () => {
    return (
        <section>
            <Navbar></Navbar>
        </section>
    );
};

export default Home;